## What & why

<!-- What does this change do, and what's the motivation? -->

## Changes

<!-- Bullet the key changes. -->
-

## Checklist

- [ ] `uv run ruff check .` passes (from `backend/`)
- [ ] `uv run pytest -q` passes (from `backend/`)
- [ ] `npm run build` passes (from `frontend/`)
- [ ] Added/updated tests for behavior changes
- [ ] Updated docs/README if behavior or config changed
